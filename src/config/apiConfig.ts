// src/config/apiConfig.ts
export const API_ENDPOINTS = 'http://localhost:3000';

export const API_RESSOURCE = {
  businessCustomers: `${API_ENDPOINTS}/business-customers`,
  products: `${API_ENDPOINTS}/products`,
  inventory: `${API_ENDPOINTS}/inventory`,
};
